import 'package:video_player/video_player.dart';

/// Web sapı: yerel dosya yolundan video oynatma yok (galeri seçici zaten
/// web'de açılmıyor). null → önizleme küçük resimde kalır.
VideoPlayerController? yerelVideo(String yol) => null;
