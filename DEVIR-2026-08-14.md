# dizi.jpg — Devir Notu (14 Ağustos 2026)

Bu dosya, oturum değiştiren bir ajanın **tek dosyadan** devralması için yazıldı.
Ayrıntılı tarihçe `YAPILACAKLAR.md`'de (teknik) ve `../yapilacaklar`da (istek
listesi). Burada yalnız **bugünkü durum** ve **sıradaki işler** var.

---

## 0) ÖNCE BUNU YAP

1. `dizijpg-ux-kontrol` skill'ini yükle (Skill aracıyla).
2. `CLAUDE.md`'yi oku — 9 madde, hepsi zorunlu.
3. UI'ya dokunacaksan `ui-ux-pro-max` skill'ine danış.

---

## 1) BUGÜNKÜ DURUM

| Kanal | Sürüm | Not |
|---|---|---|
| **Web** dizijpg.com | **1.48.0+96** | güncel, `main.5639d9ac25b3.dart.js` |
| **Backend** | **1.47.0+95** | değişmedi (sezon ucu zaten beyaz listede) |
| **APK** (elle kurulan) | 1.46.0+93 | `~/Desktop/dizijpg-1.46.0+93.apk` |
| **AAB** | 1.46.0+93 | `~/Desktop/dizijpg-1.46.0+93.aab` — **YÜKLENMEDİ** |
| **Play (Alpha)** | 1.40.0+86 | 7 Ağustos'tan beri; arada 6 derleme birikti |

* Testler: arama kişi/şirket widget · backend `arama_kisi_sirket` yeşil.
* Çeviri: `'Şirketler'`, `'Senarist'`, arama ipucu (45 dil). Fince
  `Uudet seuraajat` (md. 56). **API hata SnackBar'ları** (giriş/kayıt/izleme
  çakışması/mesaj/liste/2FA/itiraz) 45 dile eklendi, `ApiHata.toString()` `.c`
  — yerelde, dağıtım yok.
* Son dağıtım: 14 Ağu 2026 (1.48.0+96 — TMDB bölüm ısı haritası + akış kartı zemini).

### Play Store — ÜRETİME ÇIKIŞ ENGELİ (en güncel bilgi)
Panel (14 Ağu, Kontrol paneli → Üretim):
* ✅ Kapalı test sürümü yayınlandı
* ✅ 12 test kullanıcısı kaydoldu
* ⏳ **12 testçiyle 14 gün kesintisiz** — "An itibarıyla 12 test kullanıcısı
  kesintisiz olarak **13 gündür** kayıtlı". "Üretime başvur" düğmesi GRİ.

**UYARI:** sayaç kesintisiz ilerliyor; testçi listesinden biri düşerse
sıfırlanabilir. **Testçi listesine dokunma.**

**Ayrıca:** Yayın özetinde 7 Ağustos'tan beri **incelemeye gönderilmemiş** bir
değişiklik duruyor: "Kapalı test - Alpha, 86 (1.40.0), tam kullanıma sunma".

---

## 2) KULLANICIDA BEKLEYENLER (ajan yapamaz)

1. **AAB'yi Play'e yükle** — `~/Desktop/dizijpg-1.46.0+93.aab` (96 MB).
   Sürüm notu HAZIR: `surum-notu-1.46.0.txt` (tr-TR 470 / en-US 445 karakter,
   Play sınırı 500). **Ajan yükleyemez**: tarayıcı yükleme aracının sınırı
   10 MB, Play Developer API kurulu değil (servis hesabı/fastlane yok).
2. **Play Veri Güvenliği beyanı** — md. 23 gerçekten yeni veri topluyor
   (görüntülenme kaynağı, video izlenme kovası). Gizlilik politikası metni
   46 dilde canlıda ama Play'deki beyan güncellenmedi.
3. Arama testi için iki telefon (arama zaten kapalı, askıda).

---

## 3) SON İKİ GÜNDE YAPILANLAR (1.41 → 1.46)

Ayrıntı `YAPILACAKLAR.md`'nin en üstünde, tarih başlıklarıyla.

* **1.41.0+88** — md. 20 Hareketlerim · md. 21 gizlilik anahtarları ·
  md. 23 gönderi istatistikleri · md. 24 toplu istatistikler + emoji
  animasyonu ve "izledim/izleyeceğim çelişkisi" hataları.
* **1.42.0+89** — md. 49 yapım ekibi/firma · md. 29 seviye · md. 36 doğum günü.
* **1.43.0+90** — md. 35(a) medya kalitesi. **Video sıkıştırma dosyayı
  BÜYÜTÜYORDU** (33,6→40,9 MB, VMAF 100→93). Fotoğraflar 2000 px'e
  kırpılıyordu. **12 MP fotoğrafla avatar hiç yüklenemiyordu.**
* **1.44.0+91** — md. 52 iki adımlı doğrulama (e-posta) · md. 53 H.264 olmayan
  kodek · md. 54 saydam PNG · Google hesap seçici hatası.
* **1.45.0+92** — video elde tutma eğrisi · istatistik zaman seçicisi
  (281→73 dp) · `'%{}'` yüzde kalıbı anahtarı.
* **1.46.0+93** — istatistik ekranları yeniden düzen (%34 kısaldı) · gönderi
  istatistik modalı + oranlar · seviye sadeleştirme (unvan ve tavan kalktı).
* **1.47.0+95** — arama kişi+şirket (cartoon → Cartoon Network, şirketler
  dizi/filmden sonra) · splash gerçek logo · brotli_static · SimpleIcons
  1,36 MB→6 KB · WebP Accept · Fince `Uudet seuraajat`.

### Aynı gün yapılan veri işlemi
**dizi.jpg resmi hesabına (id=42) 168 yapım "izlendi" işaretlendi**
(48 film + **10.708 bölüm**), 166 bitirdim + 2 izliyorum (South Park, Reacher).
`POST /durum` ucu kullanıldı (ham SQL değil); "devam edenler izliyoruma"
işini `dizi_durum.js` + `araclar/durum_duzelt.js` otomatiği yaptı.
`durumlar` tablosunun yedeği sunucuda: `/yedekler/durumlar-2026-08-13-*.sql`.

---

## 4) SIRADAKİ İŞ

PageSpeed / WebP / splash / Fince / arama kişi+şirket **CANLIDA** (1.47.0+95).
Ölçüm tablosu tarihçe için aşağıda duruyor; kod yazılacak iş değil.

**Hazır, ayrı dikkatli tur:** md. 58 nginx `@spa` (`backend/nginx-seo-20260814.parca.conf`).
Yanlış conf tüm siteyi kırar — `try_files $uri @spa;` canlıya işlemeden önce
yedek + rollback planı.

**Küçük/net ürün:** 55 bookmark · 59 profil listesi sürükle · 60 profil
sayfası görüntülenme.

**Tarif / araştırma:** 30 moderatör (kullanıcı) · 18+48 wiki/ödül (kaynak).

Aşağıdaki tablo **uygulandı** (1.47.0+95); tarihçe için duruyor.

| İş | Ölçülmüş kazanç (hepsi canlıda) |
|---|---|
| nginx `brotli_static` (paket apt'ta mevcut) | main.dart.js −810 KB + fontlar −548 KB |
| SimpleIcons'ı 18 ikona indir (paket 3.300 ikonu sabit haritada tutuyor, budayıcı kesemiyor) | −1.330 KB |
| `CachedNetworkImage`'a `Accept: image/webp,*/*` | katalog görsellerinde %28 (~450 KB/ızgara) |
| `index.html`'e statik açılış ekranı | **mobil 34→70, masaüstü 59→91** (tek başına en büyük kaldıraç) |
| `abiFilters`'tan x86_64 çıkar | APK 119,6 → 77,3 MB |
| Boot sonrası `flt-viewport` meta düzelt | erişilebilirlik 93→100 |

**DÜRÜST HEDEF: mobil ~70, masaüstü ~91 — 100 DEĞİL.** "En İyi Uygulamalar"
tavanı **81** ve tek engel `Intl.v8BreakIterator`, o da Flutter SDK'sının
derlenmiş kodunda.

**YAPMAYIN (ölçüldü, kazancı yok):** TMDB için srcset (zaten var,
`api.dart:47`), TMDB AVIF (TMDB servis etmiyor), Cloudflare Polish/Image
Transformations (ücretsiz planda YOK, `/cdn-cgi/image/` → 404), canvaskit'i
kendi origin'imize almak (fark 20 KB), Poppins ağırlığı azaltmak (6'sı da
kullanılıyor), armeabi-v7a'yı çıkarmak (32-bit cihazlar düşer).

**Kullanıcının örnek aldığı site yanılgısı:** `sinamalar.com` diye bir alan
adı YOK. `sinemalar.com` ölçüldü ve **dört kategorinin üçünde bizden KÖTÜ**
(erişilebilirlik 78 vs 93, en iyi uyg. 58 vs 81, SEO 92 vs 100). Masaüstü
performansta öndeler ama sebebi az bayt değil (25 MB, 3.552 istek indiriyorlar)
— **erken boyuyorlar**. Bizim kazanacağımız yer tam orası: statik açılış ekranı.

---

## 5) AÇIK MADDELER (`../yapilacaklar` içinde tam metin)

**Kullanıcının tarif etmesi gereken:**
* **30 Moderatör sistemi** — silme mi, gizleme mi, yalnız işaretleme mi?
  Md. 6'daki yönetici tarafının ALT YETKİ SEVİYESİ olarak kurulmalı.

**Araştırma gerektiren:**
* **18 wiki + 48 ödüller** — ikisi aynı kaynağa bağlı. TMDB ödül TUTMUYOR,
  IMDb lisanslı. Önce kaynak+lisans fizibilitesi.

**Küçük ve net:**
* **55 Gönderi kaydetme (bookmark)** — kullanıcı "kaydetme oranı" istedi ama
  özellik YOK (düğme/tablo/uç/ölçü hiçbiri). Ajan uydurmadı, bildirdi.
* **58 nginx `@spa`** — SEO soft 404 kodu canlı API'de; conf uygulanmadı.
* **59 Profil listelerini sürükleyerek sıralama** — ham nottan 14 Ağu
  numaralandı (favori / izledim / izliyorum / filmler).
* **60 Profil sayfası görüntülenme** — md. 23'teki "profil ziyareti"
  gönderiden geçiş; bu madde sayfanın kendisi.

**Biten bu tur (işaretle, tekrar açma):** 50 katalog WebP · 51 PageSpeed
dürüst tavan · 56 Fince · 57 arama kişi+şirket · LIMIT 500 `takip_ediyorum`.

**Karar bekleyen güvenlik (`[!]`):**
* DB rolünü kısıtlama — **DİKKAT:** `dizijpg` giriş yapabilen TEK süper
  kullanıcı, `postgres` rolü YOK. Yanlış sırayla yapılırsa veritabanına erişim
  kaybedilir ve SQL ile geri alınamaz. Yedek elde, AYRI turda.
* CSP report-only — nginx'te 7 ayrı yere girmeli (`add_header` miras alınmıyor).
* **Medya imzası ZORUNLU modu — AAB YÜKLENENE KADAR AÇILAMAZ.** Şimdi
  açılırsa eski sürümdekilerin DM medyası görünmez olur. Play'de 1.40.0+86 var.

**Bekletilenler (gerekçeleri listede):** 31 medya editörü + 35(b)/(c) yapay
zekâ ile kalite (ffmpeg_kit EMEKLİ, tflite native eklenti — bu projede native
eklentiler defalarca kırıldı), 32 soundtrack (veri kaynağı yok gibi),
33 arama efektleri (arama zaten KAPALI), 8 mini yarışmalar (tarif tek cümle),
5 rehber ve 44 balon grafiği (kullanıcı kararıyla en sonda), 34 kahve ısmarla
(rafta, ödeme/hukuk işi).

---

## 6) BU PROJEDE İŞE YARAYAN ÇALIŞMA BİÇİMİ

* **Kullanıcı ajanlarla çalışmak istiyor**: "işleri sen yapma, ajanlara ver."
  Paralel ajan çalıştırırken **dosya sahipliğini AÇIKÇA böl** ve her ajana
  "şu dosyalara DOKUNMA" listesi ver — bu tur 5 ajan paralel çalıştı, çakışma
  olmadı. `server.js` bölge bölge paylaştırılabiliyor.
* **ÖNCE ÖLÇ.** Bu tur ölçüm üç kez yanlış işten kurtardı: 720p kutusunu
  yükseltmek (kazanç yok), HEVC'ye özel kural (canlıda HEVC sıfır, asıl sorun
  VP9), PageSpeed karşılaştırması (örnek site bizden kötü).
* **Az veride oran gösterme.** Yerleşik eşikler: `ETKILESIM_EN_AZ_GONDERI = 3`,
  `VIDEO_KOVA_EN_AZ = 20`, `oranEnAzGoruntulenme = 50`, `YON_EN_AZ = 30`.
  Eşik altında sayı GÖSTERİLMEZ, "yeterli veri yok" denir.
* **Gizlilik sözleri:** görüntüleyen KİMLİK değil SAYI (md. 23);
  `izlenenler_gizli` bütün türevleri kapatır (md. 21); seviye 1 başkasına
  hiç gitmez (md. 29).
* **Eski istemci uyumu:** Play'de 1.40.0+86 durduğu için sunucu değişikliği
  yaparken eski alanları da döndür (örn. `/istatistiklerim/gonderiler`
  `?sirala` yoksa eski alanları da veriyor).
* **Çeviri disiplini:** yeni kullanıcı metni = AYNI TURDA 45 dile. Yeni
  anahtar açan ajan `lib/diller/`e YAZMAZ, listeyi rapor eder; çeviri ayrı bir
  turda 3 ajana bölünür. Ajanlara **kendi scratchpad alt dizinini** verin —
  paylaşılan dizinde iki ajan aynı betik adını kullanıp birbirini ezdi.
* **Yüzde yazarken `'%{}'` anahtarını kullan**, `'%$x'` diye SABİT YAZMA.
  İşaretin yeri dile göre değişiyor (tr `%45`, en `45%`, de `45 %`, fa `45٪`).

## 7) SUNUCU

* Dağıtım dizini **`/opt/dizijpg`** — `/root/dizijpg` BAŞKA bir projedir
  (Nisan'dan kalma "Dizipal Clone"). Karıştırma.
* Dağıtım ritüeli `dizijpg-ux-kontrol` skill'inde, 7. madde. SW sökücü ve
  `web_hashla.js` adımları ATLANMAZ.
* `rsync --delete` KULLANMA — `robots.txt` build çıktısında yok, silinir.
* Test hesapları: `testkullanici` / `test1234`. Resmî hesap `dizi.jpg` (id=42);
  şifreler `~/.config/dizijpg/*_sifre` altında.
* **`alcelik` (id=3) GERÇEK kullanıcıdır — verisine dokunma.**
